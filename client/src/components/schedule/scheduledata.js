/**
 * This is the array used to store the talks that the user is interested in.
 * It is it's own file to allow for other components to access easily via importing.
 */
const scheduleData = [
    
]

export default scheduleData;